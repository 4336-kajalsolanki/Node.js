<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f9;
            padding: 30px;
        }

        .container {
            max-width: 500px;
            padding: 20px;
            margin: 50px auto;
            border-radius: 10px;
            background-color: white;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        h2 {
            color: #333;
            font-size: 24px;
            text-align: center;
            margin-bottom: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        td {
            padding: 12px;
            text-align: left;
        }

        input[type="text"] {
            width: calc(100% - 12px);
            font-size: 16px;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            outline: none;
            transition: border-color 0.3s;
        }

        input[type="submit"] {
            width: 100%;
            background-color: #4CAF50;
            color: white;
            font-size: 16px;
            margin-top: 20px;
            padding: 12px;
            border: none;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        input[type="text"]:focus {
            border-color: #4CAF50;
        }

        input[type="submit"]:hover {
            background-color: #45a049;
        }
    </style>
</head>

<body>
    <div class="container">
        <h2>Edit User</h2>
        <form action="/insertRecord" method="post">
            <input type="hidden" name="editid" value="<%= single.id %>">
            <table>
                <tr>
                    <td>Task :- </td>
                    <td><input type="text" name="task" value="<%= single.task %>" required></td>
                </tr>
                <tr>
                    <td>Status :- </td>
                    <td><input type="text" name="status" value="<%= single.status %>" required></td>
                </tr>
                <tr>
                    <td>Deadline :- </td>
                    <td><input type="datetime-local" name="deadline" value="<%= single.deadline %>" required></td>
                </tr>
                <tr>
                    <td colspan="2">
                        <input type="submit" value="<%= single.id ? 'Update User' : 'Add User' %>">
                    </td>
                </tr>
            </table>
        </form>
    </div>
</body>

</html>