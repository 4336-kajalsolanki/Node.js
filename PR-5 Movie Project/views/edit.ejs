<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            color: #495057;
        }

        .form-container {
            background-color: #ffffff;
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        }

        .form-container img {
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        h2 {
            color: #343a40;
            font-weight: bold;
        }

        .btn-primary {
            background-color: #6c757d;
            border: none;
        }

        .btn-primary:hover {
            background-color: #495057;
        }

        .btn-secondary {
            background-color: #dee2e6;
            border: none;
        }

        .btn-secondary:hover {
            background-color: #ced4da;
        }

        label {
            font-weight: bold;
            color: #495057;
        }

        .form-control {
            background-color: #f1f3f5;
            border: 1px solid #ced4da;
        }

        .form-control:focus {
            border-color: #6c757d;
            box-shadow: none;
        }

        .d-grid .btn {
            margin-top: 10px;
        }
    </style>
</head>

<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-lg-6 col-md-8">
                <div class="form-container">
                    <h2 class="text-center mb-4">Edit User</h2>
                    <form method="post" action="/updateRecord" enctype="multipart/form-data">
                        <input type="hidden" name="editid" value="<%= single.id %>">
                        <div class="mb-3">
                            <label for="movieName" class="form-label">🎥 Movie_Name:</label>
                            <input type="text" class="form-control" id="movieName" name="movieName"
                                value="<%= single.movieName %>" required>
                        </div>
                        <div class="mb-3">
                            <label for="Price" class="form-label">💰 Price:</label>
                            <input type="text" class="form-control" id="Price" name="Price" value="<%= single.Price %>"
                                required>
                        </div>
                        <div class="mb-3">
                            <label for="Description" class="form-label">📖 Description:</label>
                            <textarea class="form-control" id="Description" name="Description" rows="3"
                                required><%= single.Description %></textarea>
                        </div>
                        <div class="mb-3">
                            <label for="avatar" class="form-label">📂 Upload New Image:</label>
                            <input type="file" class="form-control" id="avatar" name="avatar">
                            <div class="mt-3">
                                <label>Current Image:</label>
                                <img src="<%= single.Image %>" width="100" alt="Current Image">
                            </div>
                        </div>
                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-primary btn-lg">Save Changes</button>
                            <a href="/viewRecord" class="btn btn-primary btn-lg">Cancel</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>